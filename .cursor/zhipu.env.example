# Local compile-time dart-defines. Copy to .cursor/zhipu.env (gitignored).
# Used by flutter build/run --dart-define-from-file so the GLM key is baked into the APK.
ZHIPU_API_KEY=23fbf7dc7fd04e879e264f3728200cb4.yEVrObKExSg2D2r0
